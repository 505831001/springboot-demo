server:
  port: 8881
  servlet:
    context-path:

spring:
  profiles:
    active: dev
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    servlet:
      content-type: text/html
    mode: HTML
  application:
    name: oauth2-server
  main:
    allow-bean-definition-overriding: true
